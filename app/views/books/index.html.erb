<h1>Books</h1>


<table>
  <thead>
  	<tr>
  		<th>Title</th>
  		<th>Body</th>
  		<th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  	<% @books.each do |book| %>
  	<tr>
  	  <td> <%= book.title %></td>
      <td><%= book.body %></td>
      <td><%=link_to "Show", "/books/#{book.id}",class:"show_#{book.id}"%>
      <td><%=link_to "Edit", "/books/#{book.id}/edit",class:"edit_#{book.id}"%>
      <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure?",class:"edit_#{book.id}"%>

  </tr>
  <% end %>
    </thead>
</table>
<h1>New book</h1>
<%= form_for(@book) do |f| %>

    <h4>Title</h4>
    <%= f.text_field :title,class: 'book_title' %>

    <h4>Body</h4>
    <%= f.text_area :body,class: 'book_body'  %><br>

    <%= f.submit 'Create Book' %>
<% end %>